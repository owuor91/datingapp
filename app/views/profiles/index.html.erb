<p id="notice"><%= notice %></p>

<h1>Listing People</h1>

<table>
  <thead>
    <tr>
      <th>Phone</th>
      <th>Occupation</th>
      <th>Education</th>
      <th>Hobbies</th>
      <th>Bio</th>
      <th>Location</th>
      <th>Relationship</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |person| %>
      <tr>

        <td><%= person.phone %></td>
        <td><%= person.occupation %></td>
        <td><%= person.education %></td>
        <td><%= person.hobbies %></td>
        <td><%= person.bio %></td>
        <td><%= person.location %></td>
        <td><%= person.relationship %></td>

        <td><%= link_to 'Show', person %></td>
        <td><%= link_to 'Edit', edit_profile_path(person) %></td>
        <td><%= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
